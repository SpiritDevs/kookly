import { SectionPageContent } from "@/components/section-page-content";

export default function TicketIssuesPage() {
  return (
    <SectionPageContent
      title="Issues"
      heading="Track the tickets that need active resolution."
      description="Keep current problems visible here so the team can work through blockers, updates, and next actions in one queue."
    />
  );
}
