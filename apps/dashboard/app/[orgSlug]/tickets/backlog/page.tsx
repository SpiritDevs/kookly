import { SectionPageContent } from "@/components/section-page-content";

export default function TicketBacklogPage() {
  return (
    <SectionPageContent
      title="Backlog"
      heading="Hold lower-priority ticket work without losing it."
      description="Use the backlog for ideas, deferred fixes, and requests that matter, but do not need immediate attention."
    />
  );
}
