import { SectionPageContent } from "@/components/section-page-content";

export default function TicketTriagePage() {
  return (
    <SectionPageContent
      title="Triage"
      heading="Sort incoming tickets before they slow the team down."
      description="Use triage to review urgency, assign ownership, and decide what should move straight into active work."
    />
  );
}
