import { SectionPageContent } from "@/components/section-page-content";

export default function LeadsUnassignedPage() {
  return (
    <SectionPageContent
      title="Unassigned"
      heading="See which leads still need an owner."
      description="Use this queue to claim new opportunities, keep response times tight, and prevent promising leads from sitting untouched."
    />
  );
}
