import { SectionPageContent } from "@/components/section-page-content";

export default function LeadsSpamPage() {
  return (
    <SectionPageContent
      title="Spam"
      heading="Keep low-quality or unwanted leads out of the main queue."
      description="Review suspicious or irrelevant inbound messages here so the primary leads workflow stays focused on real opportunities."
    />
  );
}
