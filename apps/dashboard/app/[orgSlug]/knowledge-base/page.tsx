import { SectionPageContent } from "@/components/section-page-content";

export default function KnowledgeBasePage() {
  return (
    <SectionPageContent
      title="Knowledge Base"
      heading="Keep internal answers and reusable guidance in one place."
      description="Use this space for playbooks, macros, and product context so the team can respond faster with consistent information."
    />
  );
}
