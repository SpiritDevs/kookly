import { SectionPageContent } from "@/components/section-page-content";

export default function ContactsPage() {
  return (
    <SectionPageContent
      title="Contacts"
      heading="Manage the people and companies tied to your workspace."
      description="Use contacts to keep account context organized across individuals, organizations, and the conversations connected to them."
    />
  );
}
