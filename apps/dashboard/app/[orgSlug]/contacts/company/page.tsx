import { SectionPageContent } from "@/components/section-page-content";

export default function ContactCompanyPage() {
  return (
    <SectionPageContent
      title="Company"
      heading="Keep company-level context close to the work."
      description="Use this section for organization details, shared notes, and the account history that gives every conversation more context."
    />
  );
}
